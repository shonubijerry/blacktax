{
  "name": "blacktax",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
    "format": "prettier  --config .prettierrc --write '*.{json,js}' 'src/**/*.{js,ts}'",
    "lint": "eslint . --max-warnings 0 --fix",
    "dev": "concurrently -n 'UI,API' -c auto 'cd ui && npm run dev' 'cd api && npm run dev'"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "api",
    "ui"
  ],
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.5.2"
  }
}
